# TODO

- [ ] Fix Filament v2 resource method signature for AppointmentsResource.php (table() type mismatch)
- [ ] Run `php artisan optimize:clear` and `composer dump-autoload` to ensure fatal error is gone
- [ ] If more resources have the same signature issue, fix them too

